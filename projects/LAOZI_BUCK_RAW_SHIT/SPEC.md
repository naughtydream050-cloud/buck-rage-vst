# 老子-BUCK RAW SHIT- Spec

- Visual truth: `ui/reference/reference.png`.
- UI SSOT: `ui/spec/ui-spec.json`.
- Phase 1: Windows VST3 UI and interaction validation only.
- Audio path: input → optional output gain → stereo meter → output. Bypass leaves input unchanged.
- No mastering DSP, macOS build, Developer ID signing, or FL Studio host-validation claim in this phase.
